# Unreleased

- Remove UI color overrides so the RUNE theme only applies token colors; it no longer forces an editor background/foreground.
- Add `constant.color.rune` scope to highlight hex palette entries (e.g., `#RRGGBB`) used by `Ontology:Palette` in `hex.rune`.
- Theme now styles color constants in a distinct hue to make palette blocks easy to scan in the editor.
